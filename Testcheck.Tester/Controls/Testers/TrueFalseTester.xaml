<UserControl x:Class="TAlex.Testcheck.Tester.Controls.Testers.TrueFalseTester"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:TAlex.Testcheck.Tester.Converters">
    <UserControl.Resources>
        <conv:BoolInverterConverter x:Key="BoolInverterConverter" />
    </UserControl.Resources>
    
    <Grid>
        <StackPanel Margin="5">
            <RadioButton IsThreeState="True" IsChecked="{Binding ActualChoice, UpdateSourceTrigger=PropertyChanged}" Margin="0,1,0,1">True</RadioButton>
            <RadioButton IsThreeState="True" IsChecked="{Binding ActualChoice, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BoolInverterConverter}}" Margin="0,1,0,1">False</RadioButton>
        </StackPanel>
    </Grid>
</UserControl>
